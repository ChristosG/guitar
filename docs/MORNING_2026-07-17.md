# Καλημέρα boss — full session summary (this supersedes the older TL;DR below)

**Everything you asked for across the whole session is built, tested, and live — with ONE honest exception
I could not fully close: Gallagher's canon citations.** 41 commits. Read the four sections, then the one
open item.

## 1. The prompt screen (your #1 ask) — DONE, live at `/el/settings`
Every prompt the app sends to a model, shown verbatim in English with plain-Greek explanations beside
them. **30 of 32 are fully editable** with restore-to-default behind a confirmation modal; your edits are
what the model uses until you restore. Source-code paths are hidden from the tutor. The language "bug" you
spotted was a real **viewer bug** (the preview tied language to your UI locale; the real curriculum ties it
to the course/student) — fixed. It also surfaced a product fact: **131 of 155 lessons are English because
student Giannis's `preferred_language` is `en`** — your call whether that stays.

## 2. All 5 books re-OCR'd by Claude — DONE
3 of 4 new books were scans carrying inherited Tesseract that lost every fraction glyph ("¼-inch" →
"4-inch"). All re-read via `claude -p`: **816 vision calls, ~$35 subscription-equiv.** `¼-inch` verified
correct. Hunter has 7 unreadable glossary pages (Claude's content filter false-positived on dense
definitions — benign, least-critical content). Gallagher is a metric book (no ¼, uses mm — fine).

## 3. The concept canon (the "brain") — BUILT and visible at `/el/canon`
Compiles each book into concepts with cross-book citations, and **surfaces where authors DISAGREE as the
headline** — the thing many books buy that one can't. All 8 pieces: model, compile, reconcile, divergence
render, threshold routing (canon swaps in above 300K tokens), compile-at-ingest, Library UI, and concept
search (library + a `search_concepts` chat tool). **314 concepts, 616 claims, 5 books compiled.**

## 4. THE OPEN ITEM — Gallagher's citations are off, and I want you to decide the fix
I found the canon was citing the **printed page number** on each scan instead of the true page — off by
each book's front-matter offset. I fixed it structurally (`strip_printed_folio`, committed) and it worked
for **4 of 5 books** (Kahn, Hunter, Getting Great, Powers — citations now land on the right page, proven).
**Gallagher still resists it.** It's the longest book (388pp) with the biggest offset (24pp), and even with
folios stripped the model reconstructs the printed numbering over that much context. A claim about the
"Lacey Act" cites p.28, but that content is on physical page 52.

I **stopped spending your subscription** chasing it (another `claude -p` recompile already reproduced the
problem). My recommendation, in order:
1. **Recompile Gallagher with a real API key** — the API model may hold citation discipline better than
   `claude -p` over 366K tokens. Cheapest test. (Also makes everything ~3× cheaper / 10× faster.)
2. If that doesn't hold: **quote-based citations** — have the model emit a short verbatim quote per claim
   and resolve the page ourselves by matching it. Model-independent, immune to numbering confusion, kills
   hallucinated pages too. A real task, speced in the ledger.

I did NOT heuristic-correct Gallagher (its offset isn't perfectly constant), because a confidently-wrong
citation is the one thing this codebase refuses to ship. The other 4 books are trustworthy.

**Total subscription spend this session: ~$55-60 equiv.** Gate: **1598 passing** (the folio commit).

---
---
*(older overnight TL;DR below, from before the canon and prompt work)*

---

## The headline finding, and I got it wrong twice before I got it right

I first told you your books had "publisher-quality text layers" and ingest was free. **Wrong.** Three of
the four are 360dpi **scans carrying someone else's Tesseract OCR** (`GlyphLessFont` — the invisible
layer OCR tools lay over a scan). Only Powers is real digital text.

The tell was in my own first sample and I explained it away: I quoted Gallagher p.60 back to you
containing `[> How the base plate is formed` and called it "a bullet glyph artifact". It's a mangled `▶`.
There are 37 more in the first 120 pages.

Then I measured what that inherited OCR actually costs you:

| | |
|---|---|
| Body prose | **Excellent** — Hunter p.57: 1 error in 3,669 chars |
| **Fraction glyphs** | **100% loss.** Zero `¼½¾` survive in 2,012,859 chars |
| Figures/diagrams/photos | **Nothing.** Structural — OCR cannot describe a picture |
| Damaged pages | 9/617 render blank in **both** MuPDF and poppler; Tesseract wrote `7 ipgges x a Rar ek en ee` for one, and the pipeline ingested it as content |

`¼-inch` is *the* guitar connector. Tesseract wrote **`4-inch stereo cables`** — a confident, plausible,
wrong fact in the exact vocabulary your library exists to teach, and unfixable by regex because some
"4-inch" references are real (a 4-inch speaker dust cap). The only thing that can tell them apart is
looking at the page.

**Proven fixed, live:** Kahn p.63 now reads **`¼-inch` ×3**.

## What a tab page looks like now

Before: 502 characters of caption over a picture nothing could read. After:

> *"Standard notation staff in treble clef, 3/4 time, four measures... Below that, a 6-line TAB staff
> shows a single `0` on each string per measure, stepping from the low E string up to the high E string
> and back down, all `0` frets, framed by repeat barlines at both ends."*

That's your exercise — readable, teachable, citable to p.11. **43 of Powers' 57 pages were like that.**

**Final result — a clean sweep, zero failed, zero garbage:**

```
text_layer+claude | image_region  | 43   <- publisher text KEPT + [FIGURE] description ADDED
text_layer        | (null)        | 11   <- prose, taken FREE, correctly never re-read
claude            | no_text_layer |  3
                                    ──
                                    57
```

**43 of 57 — exactly the number the static analysis predicted before any model ran.** The 11 prose pages
kept their `½` glyphs *because* we didn't re-transcribe them. Free stays free.

Measured: **31,873 → 92,934 chars (2.9×)**, 111 chunks, **69 vision calls, $4.06, $0.059/page**.

---

## THE DECISION FOR YOU: your API key

You said "use my claude -p from my subscription for now". It works — but I measured it, and the numbers
argue against it for the remaining 831 pages:

| Path | $/page | 831 pages | Wall clock |
|---|---:|---:|---:|
| `claude -p` (subscription) | **$0.058** *(measured, 62 real calls)* | ~$48 | **15–30 hours** |
| API (`OCR_PROVIDER=claude`) | ~$0.0175 | **~$15** | 1–2 h (~40 min batched) |

Why the gap is structural, not a mistake: **`claude -p` is an agentic loop, not one call.** It reads the
image, zooms in on glyphs (we watched it: *"at 400% zoom the glyph is unambiguous"*), re-reads, and
carries Claude Code's own system prompt every turn. Several images, several turns, per page.

**Task 6 built the switch for exactly this** — set `OCR_PROVIDER=claude` and nothing else changes.
Your stored key is currently a **placeholder** (it decrypts to a string ending `-key`; Anthropic rejects
it), which is also why I couldn't run the earlier cost spike.

My recommendation: paste a real key, then I run all four books in ~1–2 hours instead of two days.

---

## What landed (12 commits, gate 1124 green)

| Task | What |
|---|---|
| 1 | `textlayer.py` — route on the **font**, not on coverage or text length |
| 2 | `page.text_source` / `ocr_reason` — provenance, additive migration |
| 3+3b | `paginate.py` stops inheriting Tesseract; digital pages with raster art still get read |
| 4+5 | Bridge `/v1/vision` — `claude -p --tools Read`, fenced; `vision()` stops delegating to Qwen |
| 6 | `OCR_PROVIDER` independent of `LLM_PROVIDER` (the API switch) |
| 7 | `fits` **actually gates** now |
| 8 | Library searched **before** the named-song decline |
| 9 | Explicit, resumable re-OCR at 150dpi; figure descriptions; merge-not-overwrite |

## On the copyright rule — you said you trust me, so here's what I did and why

I did **not** remove it. Three things I found:

1. **It restricts none of your books.** The whole library ships in the prompt and is quoted and cited
   freely. That worry was unfounded.
2. **It's an anti-hallucination guard wearing a copyright coat.** Its own message leads with *"I don't
   actually have it memorized"*. Deleting it doesn't unlock a correct tab for a famous riff — it unlocks
   a **confidently wrong one**, handed to a teacher, handed to a student.
3. **But your complaint pointed at a real bug**: `loop.py:601` declined *before* `loop.py:608` searched
   your library. A transcription you **own** was refused unread. That's what you were feeling.

Fixed by reordering (search first, decline only on a miss) and dropping "(and possibly copyrighted)" from
the message as noise for a private app. Still yours to overrule — just know it produces wrong riffs, not
permitted ones.

## Where you were right and I was wrong

- **"why to fire based on the len(text)?! some pages might have only 1 paragraph"** — correct, and it
  killed my heuristic. Length carries no signal. Powers p.11 is 502 chars against a 588 median and is
  still 75% tab. The signal is *images present*.
- **"can we also get the canon so we don't pay twice?"** — right that something was being read twice,
  wrong about what. The compile reads *text* from Postgres, never images. So there's no double payment —
  but your instinct made me put the **figure descriptions into the OCR pass**, where the image is in
  hand. That's real, and it's why the tab pages are readable at all.
- **"use haiku when u need to read something"** — Haiku's window is 200K; Gallagher is 271K and doesn't
  fit. You then said Sonnet 5 for everything, which is what ran.

## Things my own briefs got wrong that subagents caught

Worth knowing, because they're the reason this works:

- My `/v1/vision` spec **could not have worked**: `--bare` kills the subscription; `--add-dir` is variadic
  and ate the prompt; `./media:/media:ro` is the wrong filesystem (the api uses a *named volume*) — it
  would have looked mounted and 400'd every call with both containers healthy.
- **`--tools Read` is not a security fence.** Claude read `/etc/hostname` from the container that mounts
  your real `~/.claude`. Fixed with `--permission-mode manual` + a deny over `~` and `/proc`. The best
  catch: a probe asking for `.credentials.json` came back clean and **proved nothing** — that was the
  model declining, not the sandbox refusing. A safety-trained model is not a security control.
- My named-song reorder was **a no-op as briefed**: `_is_content_bearing` skips retrieval for any turn
  containing "tab" — the trigger word itself.
- My `OCR_PROVIDER` sketch would have handed a `claude` provider an **empty API key** in exactly the
  target scenario.
- A pre-existing production bug fixed in passing: `_classify` grepped `json.dumps(payload)`, so
  `"429" in "4291"` turned a **4.291-second** failure into a `rate_limit` that `jobs/runner.py` requeues
  **forever**. The 888-page run would have hit it repeatedly.

## The final whole-branch review found 2 Criticals — both fixed before the run

Verdict was "Ready with fixes". Both shared one shape: **a task changed one half of a contract and
nobody owned the other half** — exactly what a per-task review structurally cannot see.

**Critical 1 — the `[FIGURE]` contract was false on 845 of 888 pages.** The module states a positional
contract ("everything from the first marker on is ours"), but the transcribe prompt asks for markers
*interleaved* in reading order **with no end marker**. So a later reader either discards real book prose
as ours, or **cites our description of a photo as the author's verbatim words with a real page number** —
the exact failure this codebase is built against, named four lines above the contract that broke it.
Not a runtime bug: a **permanent data-format** bug the 888-page run would have written in forever.
Fixed now = one prompt change. Fixed later = re-read everything. **Timing was luck, not planning.**

**Critical 2 — `ingest.py:153` re-adopted the Tesseract layer ~30 lines after `paginate.py` refused it**,
into the chunk store BM25 and vector search actually read. The review corrected my ledger in *both*
directions: a clean re-OCR fully heals a page (I was too pessimistic), but pages ending `empty`/`failed`
keep Tesseract chunks **permanently** while `Page.text` is NULL — and an `empty`-only casualty **rolls the
book up green**. Silent, citable, wrong.

**And it found why Critical 2 shipped:** `test_ocr.py:196-202` asserts exactly the right invariant and
**can never fail** — every OCR test builds Page rows by hand, so no ingest-time chunk ever exists and the
`else` branch passes against an empty set. A test that encodes the right rule and cannot detect its
violation. Fixed with a fixture that goes through `ingest_source` first.

Fixes: `b716e67`, `972f76d`, `2a8454a`, `1099846`. **Gate 1147 passing (+23, 0 regressions).**
**Powers' 43 pages do NOT need re-reading** — the contract was made *total*, matching the describe-mode
rule that was already sound; verified 0/46 misread.

Two things the fixer caught that the review itself missed:
- The review's own Critical 2 fix was **narrower than its diagnosis** — it named three residue paths and
  covered two; the **429-parked page** kept its chunks. That path matters: an 888-page run against a
  5-hour cap hits it repeatedly. Evicting at pickup covers all three.
- **The ledger's own Task 9 proof was the live bug.** My p.73 "proof" of figure descriptions was the
  interleaved case, unterminated.

### The security fence: verified, and it holds
The reviewer didn't take the code's word — it replicated the argv and attacked three ways, all refused:
a read outside the workspace, **a symlink inside the pre-granted workspace** (the real bypass vector), and
the deny rule with `--permission-mode manual` swapped for `bypassPermissions` to isolate which fence was
doing the work. Conclusion: **the deny-list is harness-enforced, not advice to the model.** So prompt
injection from a book page is orthogonal — a malicious page can persuade the model to *try*; the harness
still refuses. The cache invariant was also verified byte-for-byte, not reasoned.

## Two more fixed after the report (and one of them was my own fault)

**`b886bd5` — source status was lying.** It said `ready` while pages were still unread. I caught it live
mid-run: Powers showed `ready` with 9 pages pending. The worse case: because upload correctly does *not*
auto-start OCR, a freshly uploaded scanned book sat at `ready` with **zero pages read** and its raw
Tesseract chunks live in search — indefinitely, with nothing telling you to press the button. `partial`
already existed in `SOURCE_USABLE_STATUSES` and was simply unused. No migration; all 8 sources classify
correctly; no backfill.

**`5449d97` — that fix created a footgun, and the subagent closed it instead of just reporting it.**
`partial` had only ever meant "mostly read, a few pages failed", so its UI is an amber badge and a
**no-confirmation** "Retry failed pages" button — fine for 3 pages. My change made `partial` reachable in
a new shape: a *never-read* book, where that same one-click button starts a **full 8–12 hour run**. That
is exactly the harm `1ef6b2d` fixed at upload, reintroduced through the UI — and it was about to be the
landing state of Hunter, Gallagher and Kahn. Now the two shapes are distinguished and the friction matches
the cost: a never-read book routes through the confirming flow and reads as an invitation to start, not an
error to fix. Web rebuilt and redeployed (the 19h-stale-bundle lesson).

**Gate: 1149 passed, exit 0. 22 commits.**

## Open, and honest about it

1. **Your existing 7 sources can't be rescued by the button.** They predate the routing and are `ready` —
   and `ready` isn't a pickup status, by design. They must be **re-uploaded**, not re-OCR'd. That includes
   Getting Great Guitar Sounds.
2. **Source status lies.** `knowledge_source.status` says `ready` while pages are still `pending`. A
   scanned book uploaded and never re-OCR'd reads "ready" with its raw Tesseract chunks live in search,
   and nothing tells you to press the button. The model *has* a `partial` status that isn't being used.
   Small fix, real consequence.
3. **The `[FIGURE]` contract is written but nothing reads it yet.** Part B's canon must be told that
   everything after a marker is a *description*, not a quotation — or it will cite a description as the
   book's own words, which is the "citation he will trust" failure this codebase is built against.
4. **Part B (the canon) is not started.** It's a real project and deserves its own careful build, not a
   rushed 5am hack. The plan for it is written and approved.
5. **Task 12 (tabs → playable artifacts) not started** — but the figure descriptions coming out are
   structured enough (`"0-0-0-0 on the low E line, then 0-0-0-0 on the A line"`) that it now looks clearly
   feasible.
6. The 300K threshold is still **reasoned, not measured**. The experiment is specced.

## Suggested order when you're up

1. Paste a real API key → I run the other 4 books (~1–2h, ~$15).
2. Fix the source-status lie (small, and it's actively misleading).
3. Part B: the canon, as its own build.
4. The Create-a-curriculum E2E gate, once the books are actually in.
