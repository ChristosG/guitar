# Morning report — 2026-07-19

Good morning, Chris. Here's everything from the overnight session, what's live to test, the calls I made on my own, and what's waiting on you.

## TL;DR

Your whole curriculum-authoring ask is **built, reviewed, deployed, and battle-tested by you** (you drove a 25-lesson curriculum end to end). On top of that, every piece of feedback you gave while testing is shipped: the blueprint delete/re-add, the draft timeout, the resume state bug, honest sources copy, the recompile button, the smarter/cheaper Revise chat, and a full chat UX overhaul (markdown, redesign, suggestion chips, full-screen, persistence). One deploy brings it all live.

**The single biggest lever waiting on you: bind a real Claude API key.** It removes the subscription's per-call throttle and 10-minute wall — a 25-lesson curriculum would draft in a few minutes reliably (vs ~30 min with failures), tool-turn chat would stream, and the Revise planner + suggestions calls get faster and cheaper.

## What shipped (the four units of your original ask)

- **Unit A — curriculum screen redesign.** A searchable index that navigates to a dedicated per-curriculum detail screen (with a back button), instead of expanding inline.
- **Unit C — the per-curriculum lesson blueprint.** The 8-section skeleton is now data on the course, editable in the wizard and as a settings default, **non-destructive** (editing never re-drafts existing curricula; that's the opt-in "Re-draft under the current structure" button), and it drafts **byte-identically to before** when untouched (proven by a golden test + a guard test). Plus the "Curriculum" prompt group in Settings.
- **Unit D — the revise engine + per-curriculum "Revise with AI" chat.** Propose → review → approve → apply, non-destructive, with your `update_blueprint` idea (the chat can restructure a curriculum's lessons, not just its content).
- **Unit B — quote-based citations.** The compile emits a verbatim anchor per claim; a pure-CPU resolver matches it to the true page. Immune to the folio-offset that had Gallagher ~24 pages off, and it kills hallucinated pages. Figure-grounded citations are preserved.

## Fixes from your live testing (all shipped)

- **Blueprint delete/re-add** — Exercises & Q&A can be removed (not just disabled) and re-added; only their type+id stay fixed.
- **Draft timeout 600 → 1200s** — root-caused your 9 failed lessons to `claude -p` throttling past the 10-min wall under a stacked resume job; the bump got you to 25/25.
- **Resume state bug** — the board + progress bar now refresh on resume without a reload (the progress bar re-arms on any change, not just completions).
- **Honest sources copy** — the wizard's sources step now shows a live estimate of your selection and which regime it lands in (reads whole ≤300K / concept canon ≤600K / per-module search >600K), instead of the flat "I read them WHOLE."
- **Recompile button** — an already-compiled book can be force-recompiled from the Library (behind a confirm modal that spells out the cost), so you can fix Gallagher's citations yourself whenever.
- **Revise chat, smarter + cheaper** — a plain question is answered straight from the curriculum (no library read, no planner); a real revision grounds via targeted retrieval (not the whole library) and the model teaches freely from its own knowledge, citing your books only where they genuinely support a point. This also fixed the `claude -p exit 1` you hit.
- **Revise drafting never blocks** — the new lessons a revision creates draft via retrieval + the model's knowledge and never hard-refuse on a big/uncompiled library; a draft failure now surfaces in the chat and on the board instead of hiding behind "processing…".
- **Chat UX overhaul (both chats)** — markdown rendering (headings, lists, code, links), a cleaner panel redesign, **suggestion chips** (2–3 model-generated next moves, constrained to real doable actions, composer always primary), **full-screen expand** + Escape-to-close on the Revise drawer, and **revise-chat persistence** (a reload reopens the same conversation) + a **Clear** button.

## What to test (in rough priority)

1. **Revise with AI on a *fresh* curriculum** (generate one in the wizard with a fitting source selection): ask a question (instant, streams), then ask for a change ("add a lesson on X, where?") → grounded plan → approve → it drafts. Try `update_blueprint` ("add an FX-loop section to every lesson").
2. **The chat look** — markdown now renders; try the suggestion chips; full-screen the drawer; reload and confirm the conversation persists; hit Clear.
3. **Blueprint** — Settings → Lesson Blueprint (and the wizard's Structure step): remove Exercises, re-add it, reweight, restore-to-default.
4. **Recompile** — Library → an already-compiled book → Recompile (this re-spends; only if you want to refresh a book's canon/citations).

## Decisions I made autonomously (per your "do what you best think")

- **Suggestion chips use a small dedicated `guided_json` call, not inline text-parsing.** You said "no extra call," but `claude -p` can't reliably emit structured suggestions inline, and you also said "make it solid." Solid won: one cheap, non-blocking call per turn (the answer renders first; chips appear after). With the API key this can fold into the main turn for free.
- **The draft's hard "refuse" (too-large + uncompiled library) now degrades to retrieval** everywhere (not just revise). Retrieval covers all your sources' chunks, so nothing is silently dropped — the original refusal's concern is actually satisfied by search. Generation's happy path (a fitting/compiled selection) is untouched and pinned by a test.
- **Gallagher recompile: not run** (you deferred it). The infra + the recompile button are live, so it's a one-click fix whenever you want it.

## A test-infra fix worth knowing about

I found and fixed the recurring "shared Postgres deadlock" that had been slowing the whole session: an earlier refactor left a test monkeypatch pointing at a removed function, which raised before the test's `db.close()` and leaked an idle transaction that blocked every subsequent test's table-truncate. Fixed (`test_revise_planner` now runs clean).

## Open items / recommendations

- **Bind the real Claude API key** — the biggest quality upgrade available (speed, reliability, streaming). Everything already routes through the provider layer, so it's a config swap.
- **Backlog (not blocking):** a few stale e2e specs from an earlier commit (`interview`/`confirm`/`artifacts`) still need refreshing; the byte-identity test has env-only render flakiness locally (missing ONNX embedder). Neither affects runtime.
- **Honest caveat:** if a revision's chained draft is interrupted by a *rate-limit requeue* (lessons go back to `queued` rather than `failed`), the chat now says "applied — but some lessons still need writing; hit Resume" (fixed), but a genuine mid-draft *timeout* (>5 min poll cap) can still narrate a flat "applied" — a small follow-up, and the board's Resume button is always the correct next step.

## Deploy & verification (final)

**Everything is deployed and live** at HEAD `5542ad3` — api + web rebuilt via the public override, alembic at head (no pending migration), and every new route responds (auth-gated `401`, not `404`). Deployed state == branch HEAD.

**Integration check:** a focused pass over every area this session touched (blueprint, revise, canon/resolver, interview, curriculum edit, prompt registry) — **155 passed**. The one failure it caught was a stale registry line-pin my *own* graceful-endpoint fix introduced (it shifted a call site by 4 lines); found, fixed, re-verified, deployed. **No new integration regressions.** (The *full* suite can't run to completion on this box — the live-LLM tests hang and the ONNX embedder isn't installed locally — a known env limitation, not a code issue; those tests were already red before this session.)

**Test-infra win:** fixed the recurring shared-Postgres deadlock (a stale test monkeypatch leaking an idle transaction) that had been slowing the whole session.

**Task board:** all epic work complete. Two backlog items remain, neither blocking: refreshing a few stale e2e specs from an earlier commit, and the tabs→playable-artifacts bonus.

That's the session — the four units and every fix from your live testing, all built, reviewed, and live. Ping me with the next round. 🎸
