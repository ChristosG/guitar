# Guitar Tutor Copilot — Implementation Plans

- **Spec:** [`../specs/2026-07-06-guitar-tutor-copilot-design.md`](../specs/2026-07-06-guitar-tutor-copilot-design.md)
- **Build order & rationale:** spec §9.

The system is decomposed into **per-subsystem plans**. Each produces working, testable
software on its own and is executed + verified **before the next plan is written**, so
later plans can reference the real interfaces earlier plans produced (not guesses).

| # | Plan | Status | Delivers |
|---|------|--------|----------|
| 1 | `2026-07-06-foundations.md` | ✅ **DONE** | monorepo · Docker Compose · Postgres+pgvector · FastAPI skeleton · `LLMProvider`(Qwen) · core DB models+migrations · themed bilingual Next.js shell |
| 2 | `2026-07-07-knowledge-brain.md` | ✅ **DONE** | ingest (pdf/url/text) → chunk → embed → pgvector · retrieval · grounded bilingual /ask · Knowledge UI · SSRF-guarded |
| 3 | `2026-07-08-curriculum-segmentation.md` | ✅ **DONE** | guided-JSON generate (Brain-grounded) · deterministic segmentation · deep-clone assign · **cockpit shell** + Students + Curricula card board |
| 4 | `2026-07-08-artifact-engine.md` | ✅ **DONE** | spec → validate → SVG: chord diagrams · tab/staff (AlphaTab) · tone-recipe/signal-chain/amp-dial cards · LLM spec-gen · Artifacts gallery + attach-to-segment |
| 5 | `2026-07-10-agent-tools-hitl.md` | 🔄 **T1–T6 DONE, e2e-verified** (go-live T7 pending) | hand-rolled ReAct loop over `LLMProvider.chat_tools` (native Qwen tool-calling, NOT LangGraph/SSE — see plan's own "reverses stale spec decisions" note) · ~13-tool registry (6 reads + 7 mutations) · Postgres HITL (`ChatSession`/`Message`/`ApprovalRequest`, approve-before-execute) · chat router (messages/resolve/pending) + async-generate compose (reuses Plan 8's job/poll) · chat UI + approval card — verified live: real model tool-calls correctly at the full roster (read intent answers without hallucinating a mutation; mutation intent proposes the right tool + suspends) and a real browser e2e (grounded Q&A → cited-quality answer; curriculum-gen approval → job → tree; chord-diagram approval → artifact); remaining: T7 go-live (shared with Plan 8) |
| 6 | `2026-07-10-cockpit-integration.md` | 🔄 **T1–T4, T6 DONE** (T5 Today/Prep pending) | `Note` model+CRUD+promote-to-Brain · Notes cockpit page · Progress/LessonLog services + student-detail aggregate · student-detail page + assign-curriculum UI · the 3 deferred agent tools (`add_note`/`promote_note_to_knowledge`/`log_progress`) wired into Plan 5's `TOOLS` registry as `kind="mutation"` (same HITL suspend/approve path, unchanged) — verified live: non-live suspend + DB-backed unit tests (356 passed) · live-LLM test (`add_note` proposed correctly at the now-16-tool roster) · full browser e2e across the new cockpit (create+promote a note → appears in Knowledge; assign a curriculum + set a progress status + log a lesson on a student's detail page; a chat turn proposing `add_note` → approval card → Approve → confirmed persisted) — stack rebuilt+healthy throughout; remaining: T5 Today/Prep page |
| 7 | `2026-07-10-seed-content.md` | ✅ **DONE — live-seeded + browser-verified** | idempotent `seed.py` (Brain sources + "Guitar Tone & Gear" + "Zero to Hero" curricula + 8 showcase artifacts + sample student/assignment) · live run against the real LLM/embed (`guitar` DB, ~5 min, all steps succeeded) · verified via API/DB and Playwright (curricula render as nested boards, artifacts gallery renders all kinds, knowledge sources list, student roster) — concern: the 3 Wikipedia sources land `ready` but with 0 chars (Wikimedia's bot-policy blocks the fetch with a 403; not a code bug — see task-2-report.md) |
| 8 | `2026-07-09-async-generation-and-deploy.md` | 🔄 **T1–T5 DONE, e2e-verified** (deploy T6–T7 pending) | async `generation_job` + `BackgroundTasks` runner · `POST /curricula/generate`→202 · `GET /jobs/{id}` poll · FE poll+render — verified live (real LLM, UI+curl); remaining: nginx orange-cloud vhosts · DNS · certs · live verify on `guitar.cgrigoriadis.online` |
| 9 | `2026-07-12-library.md` | ✅ **DONE — acceptance criterion met on the real book** | page-native Brain (`Page`/`Collection`/`Chunk.page_id`) · `LLMProvider.vision()` seam (local Qwen3.5-9B VL, 110dpi) · `paginate_source` (renders page scans) · `ocr_source` (per-page commit + auto-retry + embed) · **D6: `ready` iff it really carries text — the "green but empty" lie is dead** · OCR-as-`GenerationJob` + Library/reader/media/retry API · one-level Collections · Library home + the **Reader (scan ‖ selectable text)** · `/lessons/from-selection` seam — **the tutor's real 77-page scanned book is now IN the app: 74 pages transcribed, 3 genuinely-blank, 0 failed, 194,671 chars, filed under "Tone & Gear"**; a book-only question ("what does pick thickness do to the tone?") retrieves the book's own prose from **p.19** and the cited page's **scan is one click away** (`GET /media/pages/{id}.jpg` → 200), verified against the real model + real DB (`test_library_live.py`) and walked in a real browser; the 3 Wikipedia sources now honestly show `empty` + Retry (Wikimedia TLS-blocks httpx — an honest red beats a green lie) |
| 10 | `2026-07-12-lesson-authoring.md` | ✅ **DONE — acceptance criterion met live** | Lesson = `Block(kind="lesson")`→`session`→`item`, **no new table** · async `draft_lesson_from_selection` (`GenerationJob(kind="lesson")`, guided-JSON, grounded in the tutor's exact selected passage, provenance on the root's `target_profile`) · deterministic `split_session`/`merge_sessions`/`add_session` (`partition_by_minutes`, no LLM) · 4 agent tools registered `kind="mutation"` (HITL-gated by the existing suspend gate, zero `loop.py`/`chat.py` changes) · Lesson editor UI + provenance chip · Reader's "Author a lesson from this" now drives the real 202+poll contract — **verified live end-to-end on the real book + real model**: selected the real pick-gauge/material passage on **p.21** ("Getting Great Guitar Sounds", printed header 23) → drafted a lesson genuinely grounded in that passage (2 sessions/4 items, wording traceable sentence-by-sentence to the selection, zero invented gear) → provenance chip opened the real scan of p.21 → told the agent in the real chat UI to split session 2 → it initially misread the lesson id as the session id (rejected), then correctly called `get_curriculum`, found the right session, and split it via HITL approve, verified in the editor and by direct DB query. **Bug found + fixed during this run**: `split_session`/`merge_sessions` silently deleted every item (no exception) if a caller had already loaded the target session's `.children` collection in the same DB session before calling — an ORM stale-collection cascade, not a logic error in the bin-packer; fixed with an explicit `db.expire(..., ["children"])` before delete, regression-tested both ways. 467 backend + 39 Playwright passing. |
